import React, { Component } from 'react';
import { Text, View, StyleSheet, SafeAreaView, Modal, Button, Alert, Dimensions } from 'react-native';

export default class MainEvent extends Component {

render() {
  return (
    <SafeAreaView>
      <Text>Hello World</Text>
    </SafeAreaView>
  );
}

}

const styles = StyleSheet.create({
  topBookContainer: {
    width: '100%',
    height: '50%',
  },
});